# LIVE_PLAN — живой план модуля бронирования

> Текущее состояние работы (не история). Обновляется по ходу. Легенда: ⬜ не начато · 🟦 в работе · ✅ готово · ⏸️ ждёт решения владельца.
> Карта проекта — `PROJECT.md`. ТЗ — `TZ.md`.

**Обновлено:** 2026-06-07

---

## Фаза 0 — Подготовка и решения ✅
- ✅ Изучён модуль бронирования донора (`azov-resort`): схема, шахматка, номера, форма брони.
- ✅ `.gitignore` (исключён `azov-resort/`), `CLAUDE.md` (правила работы), `PROJECT.md`, `LIVE_PLAN.md`.
- ✅ Обсуждены ключевые развилки: Tilda → поддомены; группа = «Объект»; мульти-сайт → свой URL на Объект; объём → только бронь/номера/настройки.

## Фаза 1 — Скелет проекта 🟦
- ✅ `package.json` (вычищенные зависимости: без recharts/tiptap/nodemailer/VK/аналитики).
- ✅ `prisma/schema.prisma`: + `PropertyObject`, `Room.objectId`, `AdminUser`; убран `BLOCKED`; убраны SEO-поля и булевы флаги удобств у Room; выпилены блог/территория/отзывы/услуги/аналитика.
- 🟦 Конфиги (next/tailwind/tsconfig/postcss) — следующий шаг.
- ⬜ `lib/`: db, utils, pricing, settings, admin-auth, media (порт + чистка; email НЕ переносим).
- ⬜ `.env.example`, `docker-compose.yml`, `Dockerfile`, базовый `nginx/`.

## Фаза 2 — Витрина (booking.*) ⬜
- ⬜ Публичная страница **Объекта** (список его номеров) — по аналогии с `/rooms`, переработанная: блок фото шире, кнопка «Подробнее» убрана, «Забронировать» ведёт на страницу номера.
- ⬜ Страница номера + `BookingForm` (карточка с календарём бронирования).
- ⬜ **Фикс бага календаря** (вчерашний день нельзя выбрать).
- ⬜ Галочка согласия с документами (из настроек) — без неё бронь не создаётся.
- ⬜ Подвал (реквизиты/документы из настроек), кнопки «На главную»/«Назад» (ссылки из настроек).

## Фаза 3 — Админка ⬜
- ⬜ Авторизация (JWT) + `AdminUser` в БД, роли ADMIN/STAFF, login-страница.
- ⬜ Вкладка **Номера**: CRUD Объектов и номеров (объект → номера; cascade-удаление).
- ⬜ Вкладка **Бронирования** (шахматка): Объекты как сворачиваемые группы; статус одной строкой; «Удалить бронь»; без «Заблокировано».
- ⬜ Вкладка **Настройки**: документы, подвал/реквизиты, навигация карточки, контакты/адрес, управление админами/сотрудниками.

## Фаза 4 — Деплой и интеграция с Tilda ⬜
- ⬜ Nginx: поддомены `booking.*` / `admin.*`, SSL (Let's Encrypt).
- ⬜ Инструкция по подключению кнопок на Tilda (ссылки на страницы номеров/объектов).
- ⬜ `deploy.sh`, проверка сборки на сервере.

---

## Решено (бывшие открытые)
- ✅ Поля номера: убрать SEO и 4 флага удобств → единый список; этаж/площадь оставить.
- ✅ Настройки: + депозит (%/фикс), время заезда/выезда; email-уведомления не делаем.
