# LIVE_PLAN — живой план модуля бронирования

> Текущее состояние работы (не история). Обновляется по ходу. Легенда: ⬜ не начато · 🟦 в работе · ✅ готово · ⏸️ ждёт решения владельца.
> Карта проекта — `PROJECT.md`. ТЗ — `TZ.md`.

**Обновлено:** 2026-06-07

---

## Фаза 0 — Подготовка и решения ✅
- ✅ Изучён модуль бронирования донора (`azov-resort`): схема, шахматка, номера, форма брони.
- ✅ `.gitignore` (исключён `azov-resort/`), `CLAUDE.md` (правила работы), `PROJECT.md`, `LIVE_PLAN.md`.
- ✅ Обсуждены ключевые развилки: Tilda → поддомены; группа = «Объект»; мульти-сайт → свой URL на Объект; объём → только бронь/номера/настройки.

## Фаза 1 — Скелет проекта ✅
- ✅ `package.json`, конфиги (next/tailwind/tsconfig/postcss), globals.css.
- ✅ `prisma/schema.prisma`: + `PropertyObject`, `Room.objectId`, `AdminUser`; убран `BLOCKED`; убраны SEO-поля и флаги удобств; выпилены блог/территория/отзывы/услуги/аналитика.
- ✅ `lib/`: db, pricing, media, utils (без BLOCKED, единый статус), settings (generic: депозит/подвал/документы), admin-auth (роли + env-админ/БД), seo (мин. alt).
- ✅ База донора скопирована и вычищена (удалены блог/территория/отзывы/услуги/аналитика/SEO/email/VK).

## Фаза 1.5 — Инфраструктура ✅
- ✅ `.env.example`, `docker-compose.yml`, `Dockerfile`, `docker-entrypoint.sh` (авто `prisma db push`), `.dockerignore`.
- ✅ `caddy/Caddyfile` (поддомены booking.*/admin.* + авто-SSL), `README.md` (деплой + Tilda). _(Решено: Caddy вместо nginx — авто-HTTPS без certbot.)_

## Фаза 2 — API-слой ✅
- ✅ Логин (env-админ ADMIN + сотрудники из БД), admin-auth с ролями.
- ✅ Объекты CRUD (`/api/admin/objects[/[id]]`, cascade, защита при бронях).
- ✅ Номера CRUD (`/api/admin/rooms[/[id]]`, objectId, без SEO/флагов, защита при бронях).
- ✅ Пользователи CRUD (`/api/admin/users[/[id]]`, только ADMIN, bcrypt).
- ✅ Брони: admin POST (без BLOCKED), публичный POST (без email/VK/аналитики, **фикс прошлой даты**).
- ✅ Админ-навигация (3 вкладки), `/admin` → `/admin/bookings`, root layout generic.

## Фаза 3 — UI ✅ (собирается целиком: `tsc --noEmit` и `next build` — чисто)
> **Решено (витрина):** URL `/o/[objectSlug]` — каталог объекта; `/rooms/[roomSlug]` — карточка+бронь; `/` — список активных объектов. Общего каталога всех номеров НЕТ. Обвязка минимальна: подвал из настроек + кнопки «Назад»/«На главную» (ведут на Tilda через `nav_back_url`/`nav_home_url`).
> **Решено (доступ):** главный админ перенесён в БД — seed из env при первом логине (`ensureSeedAdmin`), дальше логин/пароль меняются из «Настроек» через `/api/admin/account`. Сотрудник свой пароль не меняет.
- ✅ **Фикс бага календаря** в `BookingForm` (вчера нельзя, сегодня можно).
- ✅ Витрина: `(public)/layout.tsx` (подвал `SiteFooter` из настроек, без Header/Tracker), `(public)/o/[slug]` (каталог объекта), `(public)/rooms/[id]` (без seo/hasAC, кнопки назад/главная из настроек), `(public)/page.tsx` (список объектов), `RoomCard` (шире фото, одна кнопка «Забронировать» → номер), согласие из документов настроек, выпилены analytics-вызовы из `BookingForm`.
- ✅ Админ «Номера» (`AdminRoomsClient`): объекты-группы, CRUD объектов+номеров (создание номера внутри объекта), без hasAC/SEO, перенос номера между объектами, периоды цен, галерея, блокировки.
- ✅ Админ «Бронирования» (`AdminBookingsClient`): объекты как сворачиваемые группы в шахматке; единый статус (одна строка); «Удалить бронь»; без «Заблокировано».
- ✅ Админ «Настройки» (`AdminSettingsForm`): контакты+время, реквизиты подвала, документы, навигация карточки, депозит, управление доступом (свои логин/пароль + сотрудники).
- ✅ `AdminManualBookingForm`, `bookings/[id]`, `upload`, `blocked-dates` — совместимы, собираются.

## Фаза 4 — Деплой и Tilda ⬜
- ✅ Caddy-конфиг (`caddy/Caddyfile`) + инструкция (README).
- ⬜ Проверка сборки на сервере, SSL, готовые кнопки на Tilda.

---

## Что дальше (Фаза 4 — деплой)
- ⬜ Развернуть на VPS: `docker compose up`, Caddy (`/etc/caddy/Caddyfile`), DNS A-записи на поддомены.
- ⬜ Прогнать сквозной сценарий на сервере (создать объект → номер → публичная бронь → отметка оплаты).
- ⬜ Прописать у каждого Объекта `publicUrl` и повесить кнопки на сайтах Tilda.

---

## Решено (бывшие открытые)
- ✅ Поля номера: убрать SEO и 4 флага удобств → единый список; этаж/площадь оставить.
- ✅ Настройки: + депозит (%/фикс), время заезда/выезда; email-уведомления не делаем.
- ✅ Инфраструктура: Caddy вместо nginx (авто-HTTPS, без certbot).
- ✅ Витрина: каталог `/o/[obj]` + номер `/rooms/[room]`, без общего каталога, минимальная обвязка.
- ✅ Доступ: главный админ в БД (seed из env), смена своих логина/пароля из UI.
